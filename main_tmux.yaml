session_name: my_session
windows:
  - window_name: main
    layout: even-horizontal
    options:
      default-terminal: screen-256color
      mouse: "on"
    panes:
      - echo "Pane 1: Running top" && top
      - echo "Pane 2: Running htop" && htop

